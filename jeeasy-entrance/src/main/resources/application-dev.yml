server:
  # 端 口
  port: 8888
#  servlet:
#    context-path: /jeeasy

# 数据源配置
spring:
  datasource:
    dynamic:
      datasource:
        master:
          url: jdbc:mysql://localhost:3306/jeeasy?characterEncoding=UTF-8&useUnicode=true&useSSL=false&tinyInt1isBit=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
          username: jeeasy
          password: jeeasy
          driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: data.jeeasy.cn
    password: KaK6Jm55PLaJyWtW
    port: 6379
    database: 10
    lettuce:
      pool:
        max-active: 8
        max-wait: 1ms
        max-idle: 8
        min-idle: 0


mybatis-plus:
  configuration:
    # 这个配置会将执行的sql打印出来，在开发或测试的时候可以用
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

springdoc:
  api-docs:
    #是否开启文档功能
    enabled: true

